source 'https://rubygems.org'

# GitHub-Pages bundle (pins the same versions they use in production)
gem 'github-pages'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'   # Ruby ≥3 needs this
end

# Windows-only helpers
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem 'wdm',         '~> 0.1', platforms: [:mingw, :mswin, :x64_mingw]   # fast file-watcher
